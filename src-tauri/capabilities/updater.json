{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "updater",
  "description": "self-update permissions; excluded from the App Store build by build.rs",
  "windows": ["main"],
  "permissions": [
    "updater:default",
    "process:allow-restart"
  ]
}
